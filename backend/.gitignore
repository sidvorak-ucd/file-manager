# Node.js
node_modules/
npm-debug.log
Yarn.lock
package-lock.json # Usually committed, but can be ignored depending on team preference

# Build artifacts
dist/
build/

# Environment variables
.env*

# Terraform state files (should be in .gitignore in /infrastructure, but good practice here too)
*.tfstate
*.tfstate.*

# Lambda package zip (generated by Terraform)
lambda_package.zip 